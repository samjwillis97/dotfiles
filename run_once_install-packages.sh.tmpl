{{ if eq .chezmoi.os "linux" -}}

{{ if eq .chezmoi.osRelease.id "debian" -}}
#!/bin/sh

sudo apt install vim tmux zsh curl git exuberant-ctags

git config --global init.templatedir '~/.git_template'

{{ end -}}

{{ end -}}
